0. install node
1. >> npm init -y
2. go to npmjs.com and install the required packages
3. >> npm i react
4. >> npm i react-dom
5. this creates node_modules and package-lock.json for you
    In package.json --> dependencies array is created
6. we need bundler? (https://snipcart.com/blog/javascript-module-bundler)
7. We are going to user Parcel (for today) since it is easy to setup 
    and will help us build and understand react app from scratch
8. >> npm i parcel
9. run the app: 
   >> npx parcel index.html